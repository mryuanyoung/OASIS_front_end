import React from 'react';
import {Button} from 'antd';

const DataSupport = (props) => {
    return (
        <div>
            <Button>导入数据</Button>
        </div>
    )
}

export default DataSupport;